#include <iostream>
#include <string>

using namespace std;

int main()
{
string n;
cin>>n;
if(n[0]=='-')
{
if(n[n.length()-1] > n[n.length()-2])
n=n.substr(0,n.length()-1);
else
n=n.substr(0,n.length()-2)+n[n.length()-1];
}
if(n=="-0")
n="0";
cout<<n;
return 0;
}