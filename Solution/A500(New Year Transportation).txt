#include <iostream>

using namespace std;

int main()
{
	int n, t, x = 1, a;
	bool res = false;
	cin >> n >> t;
	for (int i = 1; i < n; i++)
	{
		cin >> a;
		if (i == x)
			x += a;
		if (x == t)
			res = true;
	}
	cout << (res ? "YES" : "NO");
	return 0;
}