#include <iostream>
#include<math.h>

using namespace std;

int main()
{
	unsigned long long int m, n, a, res;
	cin >> m >> n >> a;
	res = ceil((double)m / a) * ceil((double)n / a);
	cout << res;
	return 0;
}