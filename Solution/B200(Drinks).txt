#include <iostream>
#include <iomanip>

using namespace std; 

int main() 
{
	int n, x;
	double res = 0;
	cin >> n;
	for (int i = 0; i < n; i++)
	{
		cin >> x;
		res += x;
	}
	cout << fixed << setprecision(12) << res / n;
	return 0;
}