#include <iostream>
#include <math.h>

using namespace std;

int main()
{
	int x, res = 0;
	cin >> x;
	//Answer: numbers of 1 in binary show.
	while (true)
	{
		if (x == 0)
			break;
		res++;
		x -= pow(2, (int)log2(x));
	}
	cout << res;
	cin >> x;
	return 0;
}