#include <iostream>
#include <string>
#include <algorithm>

using namespace std;

int main()
{

	string a, b, s;
	cin >> a >> b >> s;
	a = a + b;
	sort(a.begin(), a.end());
	sort(s.begin(), s.end());
	cout << (a == s ? "YES" : "NO");
	return 0;
}