#include <iostream>
#include <string>

using namespace std;

int main()
{
	int n, a = 1, b = 0;
	string s, x, y;
	cin >> n;
	cin >> x;
	for (int i = 1; i < n; i++)
	{
		cin >> s;
		if (s == x)
			a++;
		else
		{
			y = s;
			b++;
		}
	}
	if (a > b)
		cout << x;
	else
		cout << y;
	return 0;
}