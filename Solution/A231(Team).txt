#include <iostream>

using namespace std;

int main()
{
	int n, x, res = 0;
	int P[1000];
	for (int i = 0; i < 1000; i++)
		P[i] = 0;
	cin >> n;
	for (int i = 0; i < n; i++)
	{
		for (int j = 0; j < 3; j++)
		{
			cin >> x;
			P[i] += x;
		}
		if (P[i] >= 2)
			res++;
	}
	cout << res;
	return 0;
}