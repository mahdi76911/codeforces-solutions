#include <iostream>

using namespace std;

int main()
{
	int n, ix, iy, iz, x = 0, y = 0, z = 0;
	cin >> n;
	for (int i = 0; i < n; i++)
	{
		cin >> ix >> iy >> iz;
		x += ix;
		y += iy;
		z += iz;
	}
	if (x == y && y == z && z == 0)
		cout << "YES";
	else
		cout << "NO";
	return 0;
}