#include <iostream>

using namespace std;

int main()
{
	int n, m, a, s = 1;
	unsigned long long int res = 0;
	cin >> n >> m;
	for (int i = 0; i < m; i++)
	{
		cin >> a;
		if (a >= s)
			res += a - s;
		else
			res += n + a - s;
		s = a;
	}
	cout << res;
	return 0;
}