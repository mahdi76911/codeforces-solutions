#include <iostream>
#include <string>

using namespace std;

int main()
{
string x,res="";
int pos=0;
cin>>x;
for(int i=0;i<x.length();i++)
{
if(i+3<= x.length() && x.substr(i,3)=="WUB")
{
i+=2;
if(res!="" && res.substr(res.length()-1)!=" ")
res+= " ";
}
else
res+= x.substr(i,1);
}
cout<<res;
return 0;
}